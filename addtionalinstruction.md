Build the M1 MVP end-to-end — FastAPI backend + classical OpenCV pipeline + React frontend — so I can run it locally and test the whole flow through the web UI: upload a form photo → auto-clean → preview → drag-corner re-crop + rotate → export color PDF.
Generate a small synthetic test set covering the hard cases: skewed, shadowed/unevenly-lit, full-frame with no margin, and rotated captures. Each test page must include color elements — a blue-ink signature and a colored stamp — so the LAB illumination stage's color preservation (FR-19) is actually tested; black-text-on-white alone won't catch it. No real PHI in the test set.
Keep the day-one PHI controls hard: strip all metadata (FR-3), no third-party egress (FR-2), upload hardening (FR-4), commercially-clean licensing — no AGPL or research-only weights (NFR-6). Defer encryption-at-rest / audit / retention for now, but don't architect anything that blocks adding them later.
Finish with clear run instructions (how to start the backend and frontend) so I can test it, then we'll move to M2.